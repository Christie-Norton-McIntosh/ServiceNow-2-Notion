# Example environment variables for SN2N proxy
# Copy to .env and replace placeholders before starting the server

NOTION_TOKEN=your_notion_integration_token_here
PORT=3004
SN2N_VERBOSE=1

# Notes:
# - Create a Notion integration and give it the required database access (Read + Update content) in Notion.
# - Put the integration token above, save as `.env` in the project root, then restart the proxy:
#     cd /Users/norton-mcintosh/GitHub/ServiceNow-2-Notion/ServiceNow-2-Notion
#     node server/sn2n-proxy.cjs
# - Keep your token private. Do not commit `.env` to git.